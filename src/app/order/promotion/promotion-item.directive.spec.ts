import { PromotionItemDirective } from './promotion-item.directive';

describe('PromotionItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PromotionItemDirective();
    expect(directive).toBeTruthy();
  });
});
