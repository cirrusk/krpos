export class Principal {
    name: string;
    uid: string;

    constructor(_name: string,
                _uid: string) {
                    this.name = _name;
                    this.uid = _uid;
                }
}
