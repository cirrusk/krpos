import { FocusBlurDirective } from './focus-blur.directive';

describe('FocusBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusBlurDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
